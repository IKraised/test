-
    var global = {
        title: 'Здесь твой тайтл',
        separator: ' | ',
        description: 'Здесь твой дескрипшен',
        lang: 'ru',
        paths: {
            images: 'img/',
            pictures: 'pictures/',
            icons() { return this.images+'symbols.svg' }
        },
        template: {
            adaptive: true
        },

        production: false
    }

    function randomInt(min,max) {
        return Math.floor(Math.random()*(max-min+1)+min);
    }

    function toPrice(x) {
        return x.toString().replace(/\B(?=(\d{3})+(?!\d))/g, " ");
    }